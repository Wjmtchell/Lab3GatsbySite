<!DOCTYPE html>
<html>
<head>
  <%- include ("../partials/header.ejs") %>
</head>

<body>

<%- include ("../partials/nav.ejs") %>

<div class="container">

<div class="users">
  <table class="users-table">
    <thead>
      <tr>
        <th> ID </th>
        <th> Username </th>
        <th> User Type </th>
      </tr>
    </thead>
    <% results.forEach(function(entry) { %>
    <tr>
        <td><%= entry.id %></td>
        <td><%= entry.username %></td>
        <td><%= entry.type %></td>
    </tr>
    <% }) %>
    
    <tr>
      <td><%= results.length + 1 %></td>
      <td><textarea name="username" id="username"></textarea></td>
      <td><textarea name="password" id="password"></textarea></td>
      <td><select>
        <option value="1">Admin</option>
        <option value="2">Teacher</option>
        <option value="3">Student</option>
      </select></td>
      <td><button type="submit">Add User</button></td>
    </tr>
  </table>
</div>

</div>

</body>
</html>
